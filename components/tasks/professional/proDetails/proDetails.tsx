import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const proDetails = ({navigation,route}) => {
  console.log("route :" ,route)
    return (
    <View>
      <Text>proDetails</Text>
    </View>
  )
}

export default proDetails

const styles = StyleSheet.create({})